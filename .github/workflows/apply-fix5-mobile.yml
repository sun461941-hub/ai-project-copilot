name: Deprecated fix5 bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Explain replacement
        run: |
          echo "The fix5 bootstrap is retired and intentionally read-only."
          echo "Use the Apply fix6 source patch workflow instead."
