diff --git a/skills/ai-project-copilot/scripts/ai_ready_bootstrap.py b/skills/ai-project-copilot/scripts/ai_ready_bootstrap.py
index 11b8fef..b4f8d09 100644
--- a/skills/ai-project-copilot/scripts/ai_ready_bootstrap.py
+++ b/skills/ai-project-copilot/scripts/ai_ready_bootstrap.py
@@ -70,7 +70,9 @@ Use the repository's own tests and CI configuration as the primary source of tru
 
 
 def _safe_write_target(repo: Path, path: Path) -> None:
-    if path.exists() and path.is_symlink():
+    # ``exists()`` is false for dangling links, but ``write_text()`` would
+    # still follow one and could create a file outside the repository.
+    if path.is_symlink():
         raise ValueError(f"refusing symlinked target: {path}")
     resolved_parent = path.parent.resolve()
     try:
diff --git a/skills/ai-project-copilot/scripts/init_project_docs.py b/skills/ai-project-copilot/scripts/init_project_docs.py
index 02d23f5..732f7d7 100644
--- a/skills/ai-project-copilot/scripts/init_project_docs.py
+++ b/skills/ai-project-copilot/scripts/init_project_docs.py
@@ -21,7 +21,9 @@ TEMPLATE_NAMES = (
 def safe_directory(path: Path) -> None:
     current = path
     while True:
-        if current.exists() and current.is_symlink():
+        # ``Path.exists()`` is false for a dangling symlink.  Check the link
+        # itself so a not-yet-created target cannot redirect later writes.
+        if current.is_symlink():
             raise ValueError(f"Refusing symlinked destination component: {current}")
         if current.parent == current:
             break
@@ -52,7 +54,7 @@ def initialize(repo: Path, output: Path, force: bool) -> dict[str, list[str]]:
         if target.exists() and not force:
             skipped.append(str(target.relative_to(repo)))
             continue
-        if target.exists() and target.is_symlink():
+        if target.is_symlink():
             raise ValueError(f"Refusing to overwrite symlink: {target}")
         shutil.copyfile(source, target)
         created.append(str(target.relative_to(repo)))
diff --git a/skills/ai-project-copilot/scripts/maintainer_triage.py b/skills/ai-project-copilot/scripts/maintainer_triage.py
index e806fc1..69435db 100755
--- a/skills/ai-project-copilot/scripts/maintainer_triage.py
+++ b/skills/ai-project-copilot/scripts/maintainer_triage.py
@@ -209,10 +209,13 @@ def main() -> int:
     parser.add_argument("--format", choices=("json", "markdown"), default="json")
     args = parser.parse_args()
 
-    if args.issue_json:
-        title, body = _load_issue(args.issue_json)
-    else:
-        title, body = args.title or "", args.body
+    try:
+        if args.issue_json:
+            title, body = _load_issue(args.issue_json)
+        else:
+            title, body = args.title or "", args.body
+    except (OSError, UnicodeError, json.JSONDecodeError, RecursionError, ValueError) as exc:
+        parser.error(f"could not read issue JSON: {exc}")
 
     result = triage_issue(title, body)
     if args.format == "json":
diff --git a/skills/ai-project-copilot/scripts/supply_chain_guard.py b/skills/ai-project-copilot/scripts/supply_chain_guard.py
index c9c47d1..5d5cbbb 100644
--- a/skills/ai-project-copilot/scripts/supply_chain_guard.py
+++ b/skills/ai-project-copilot/scripts/supply_chain_guard.py
@@ -177,7 +177,9 @@ def scan(repo: Path, manifest: Path | None = None) -> GuardReport:
         requested = manifest.expanduser()
         if not requested.is_absolute():
             requested = repo / requested
-        if requested.exists() and requested.is_symlink():
+        # Reject dangling links too: ``exists()`` is false for them while a
+        # subsequent write would still follow the link target.
+        if requested.is_symlink():
             raise ValueError("refusing to write integrity manifest through a symlink")
         target = requested.parent.resolve() / requested.name
         try:
diff --git a/tests/test_maintainer_triage.py b/tests/test_maintainer_triage.py
index 9d2edd7..429b7b6 100644
--- a/tests/test_maintainer_triage.py
+++ b/tests/test_maintainer_triage.py
@@ -3,6 +3,7 @@ from __future__ import annotations
 import json
 import subprocess
 import sys
+import tempfile
 import unittest
 from pathlib import Path
 
@@ -61,6 +62,22 @@ class MaintainerTriageTests(unittest.TestCase):
         expected = json.loads((ROOT / "examples" / "issue-triage-output.json").read_text(encoding="utf-8"))
         self.assertEqual(expected, json.loads(result.stdout))
 
+    def test_malformed_issue_json_is_a_clean_cli_error(self) -> None:
+        with tempfile.TemporaryDirectory() as temp:
+            issue = Path(temp) / "issue.json"
+            issue.write_text('{"title":', encoding="utf-8")
+            result = subprocess.run(
+                [PYTHON, str(SCRIPT), "--issue-json", str(issue)],
+                cwd=ROOT,
+                text=True,
+                stdout=subprocess.PIPE,
+                stderr=subprocess.PIPE,
+                check=False,
+            )
+            self.assertEqual(2, result.returncode)
+            self.assertIn("could not read issue JSON", result.stderr)
+            self.assertNotIn("Traceback", result.stderr)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/tests/test_repository.py b/tests/test_repository.py
index f345e83..2158acc 100644
--- a/tests/test_repository.py
+++ b/tests/test_repository.py
@@ -79,6 +79,29 @@ class RepositoryTests(unittest.TestCase):
             self.assertNotEqual(0, result.returncode)
             self.assertFalse(outside_path.exists())
 
+    def test_init_project_docs_refuses_dangling_symlink_target(self) -> None:
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            repo = root / "repo"
+            destination = repo / "docs" / "ai-project"
+            outside = root / "outside"
+            destination.mkdir(parents=True)
+            outside.mkdir()
+            target = destination / "project-brief.md"
+            try:
+                target.symlink_to(outside / "project-brief.md")
+            except OSError:
+                self.skipTest("symlinks unavailable")
+            result = run(
+                str(SKILL / "scripts" / "init_project_docs.py"),
+                "--repo", str(repo),
+                "--json",
+                check=False,
+            )
+            self.assertNotEqual(0, result.returncode)
+            self.assertIn("Refusing to overwrite symlink", result.stderr)
+            self.assertFalse((outside / "project-brief.md").exists())
+
     def test_repo_audit_emits_transparent_json(self) -> None:
         with tempfile.TemporaryDirectory() as temp:
             repo = Path(temp)
diff --git a/tests/test_v2_hardening.py b/tests/test_v2_hardening.py
index 5f15737..70ee1e0 100644
--- a/tests/test_v2_hardening.py
+++ b/tests/test_v2_hardening.py
@@ -143,6 +143,25 @@ class V2HardeningTests(unittest.TestCase):
             with self.assertRaises(ValueError):
                 guard.scan(repo, Path("../outside.sha256"))
 
+    def test_supply_chain_refuses_dangling_manifest_symlink(self) -> None:
+        guard = load_module("supply_chain_guard")
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            repo = root / "repo"
+            skill = repo / "skills" / "ai-project-copilot"
+            outside = root / "outside"
+            skill.mkdir(parents=True)
+            outside.mkdir()
+            (skill / "SKILL.md").write_text("demo\n", encoding="utf-8")
+            manifest = repo / "MANIFEST.sha256"
+            try:
+                manifest.symlink_to(outside / "MANIFEST.sha256")
+            except OSError:
+                self.skipTest("symlinks unavailable")
+            with self.assertRaisesRegex(ValueError, "through a symlink"):
+                guard.scan(repo, Path("MANIFEST.sha256"))
+            self.assertFalse((outside / "MANIFEST.sha256").exists())
+
     def test_mcp_audit_avoids_tokenizer_false_positive_and_supports_real_env_refs(self) -> None:
         mcp = load_module("mcp_config_audit")
         with tempfile.TemporaryDirectory() as temp:
diff --git a/tests/test_v2_intelligence.py b/tests/test_v2_intelligence.py
index 85f87b8..62a50b0 100644
--- a/tests/test_v2_intelligence.py
+++ b/tests/test_v2_intelligence.py
@@ -296,6 +296,22 @@ class V2IntelligenceTests(unittest.TestCase):
                 self.bootstrap.bootstrap(repo, ["copilot"], force=False)
             self.assertFalse((Path(outside) / "copilot-instructions.md").exists())
 
+    def test_ai_ready_bootstrap_refuses_dangling_target_symlink(self) -> None:
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            repo = root / "repo"
+            outside = root / "outside"
+            repo.mkdir()
+            outside.mkdir()
+            target = repo / "AGENTS.md"
+            try:
+                target.symlink_to(outside / "AGENTS.md")
+            except OSError:
+                self.skipTest("symlinks unavailable")
+            with self.assertRaisesRegex(ValueError, "refusing symlinked target"):
+                self.bootstrap.bootstrap(repo, ["agents"], force=False)
+            self.assertFalse((outside / "AGENTS.md").exists())
+
     def test_ai_ready_bootstrap_preserves_existing_policy(self) -> None:
         with tempfile.TemporaryDirectory() as temp:
             repo = Path(temp)
